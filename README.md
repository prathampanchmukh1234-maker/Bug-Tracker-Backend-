# Bug Tracker Backend

Express API for Bug Tracker Pro.

## Environment

Create a `.env` file from `.env.example` and set:

- `PORT`
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

## Local Development

```bash
npm install
npm run dev
```

Default local API URL: `http://localhost:3001`
